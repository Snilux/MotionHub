---
import Title from "../components/common/Title.astro";
import Layout from "../layouts/Layout.astro";
import AnimationBgAuth from "../components/common/AnimationBgAuth.astro";
---

<Layout title="Restablecer Contraseña">
    <div
        class="relative min-h-screen flex items-center justify-center p-4 overflow-hidden"
    >
        <!-- Background Animation -->
        <div class="absolute inset-0 -z-10">
            <AnimationBgAuth />
        </div>

        <!-- Content -->
        <div class="w-full max-w-md relative z-10">
            <!-- Logo -->
            <div class="text-center mb-4">
                <div class="flex items-center justify-center gap-2 mb-2">
                    <a href="/">
                        <Title size="lg" />
                    </a>
                </div>
            </div>

            <!-- Glass Card -->
            <div
                class="backdrop-blur-xl bg-white/50 rounded-3xl shadow-2xl p-8"
            >
                <div class="mb-8">
                    <h2 class="text-3xl font-medium mb-2">
                        Restablecer contraseña
                    </h2>
                    <p class="font-light opacity-80">
                        Ingresa tu correo electrónico y te enviaremos un enlace
                        para restablecer tu contraseña
                    </p>
                </div>

                <form id="resetForm" class="space-y-6">
                    <!-- Email Input -->
                    <div>
                        <label
                            for="email"
                            class="block text-sm font-medium text-gray-700 mb-2"
                        >
                            Correo electrónico
                        </label>
                        <input
                            type="email"
                            id="email"
                            name="email"
                            required
                            placeholder="tu@email.com"
                            class="w-full px-4 py-3 bg-white/50 border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 placeholder:text-gray-400"
                        />
                    </div>

                    <!-- Submit Button -->
                    <button
                        type="submit"
                        class="w-full bg-linear-to-r from-blue-600 to-blue-500 text-white py-3 px-4 rounded-xl font-semibold hover:from-blue-700 hover:to-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-all duration-200 shadow-lg shadow-blue-500/30"
                    >
                        Enviar enlace de restablecimiento
                    </button>

                    <!-- Back to Login Link -->
                    <div class="text-center">
                        <a
                            href="/login"
                            class="text-sm text-blue-600 hover:text-blue-700 font-medium inline-flex items-center gap-2"
                        >
                            Volver al inicio de sesión
                        </a>
                    </div>
                </form>
            </div>

            <!-- Footer -->
            <div class="mt-6 text-center text-sm text-gray-500">
                <p>© 2024 MotionHub. Todos los derechos reservados.</p>
            </div>
        </div>
    </div>
</Layout>

<script>
  document.getElementById("resetForm")?.addEventListener("submit", async (e) => {
    e.preventDefault();
    const email = (document.querySelector('input[type="email"]') as HTMLInputElement)?.value;
    const btn = document.querySelector('button[type="submit"]') as HTMLButtonElement;
    if (btn) { btn.disabled = true; btn.textContent = "Enviando..."; }

    const res = await fetch("/api/auth/forgot-password", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email }),
    });
    const data = await res.json();

    // Replace form with success message
    const form = document.getElementById("resetForm")!;
    form.innerHTML = `<div class="text-center py-4">
      <div class="text-4xl mb-3">📧</div>
      <p class="font-medium text-gray-900 mb-2">Revisa tu correo</p>
      <p class="text-sm text-gray-500">${data.message || "Si tu correo está registrado, recibirás un enlace para restablecer tu contraseña."}</p>
      <a href="/login" class="inline-block mt-4 text-sm text-blue-600 hover:text-blue-700 font-medium">← Volver al inicio de sesión</a>
    </div>`;
  });
</script>
